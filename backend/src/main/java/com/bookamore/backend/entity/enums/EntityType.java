package com.bookamore.backend.entity.enums;

/**
 * Defines available entity types.
 * -
 * During application startup, FileInitializer automatically
 * creates a subdirectory in the "uploads" root directory
 * for each enum constant.
 */
public enum EntityType {
    BOOK,
    OFFER,
    /* Add entity types here */
}
